#include "signal.h"
#include "stdio.h"
#include "unistd.h"
#include "sys/types.h"

void *signal_handler() {
    
}

int main(int argc, char *argv[]) {
    pid_t current_pid;
    current_pid = getpid();

    

    return 0;
}